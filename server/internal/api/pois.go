package api

// Handlers for the `pois` tag in contracts/openapi.yaml.

import "github.com/gofiber/fiber/v3"

// GET /api/pois
func poisQueryHandler(c fiber.Ctx) error { return notImplemented(c) }

// GET /api/pois/{id}
func poisGetHandler(c fiber.Ctx) error { return notImplemented(c) }

// GET /api/pois/categories
func poisCategoriesHandler(c fiber.Ctx) error { return notImplemented(c) }
