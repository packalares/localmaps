// Package pipeline will run the per-region build pipeline (pbf →
// pmtiles → valhalla tiles → pelias index → overture parquet) as
// described in docs/01-architecture.md. Phase 1 stubs the package.
package pipeline
